import { TimeLeftDirective } from './time-left.directive';

describe('TimeLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeLeftDirective();
    expect(directive).toBeTruthy();
  });
});
